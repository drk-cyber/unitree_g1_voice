# ===== 阶段1 核心依赖（Windows 开发机）=====
# 注：代码兼容 Python 3.8（Jetson JetPack 5）——在 3.8 环境安装时
# pip 会自动解析各库的兼容旧版（如 numpy<1.25、pydantic<2.11），无需手动锁版本。
numpy
sounddevice        # 麦克风录音 / 播放（wheel 自带 PortAudio）
soundfile          # wav 读写
pydantic>=2        # 意图 JSON 校验（附录 B）
pyyaml             # 配置文件
edge-tts           # 语音合成（需联网）
miniaudio          # mp3 解码（edge-tts 输出的是 mp3）
ollama             # 本地大模型客户端
pytest             # 单元测试

# ===== 语音识别（较重：会带上 PyTorch，下载 1GB+）=====
funasr             # SenseVoice-Small；首次运行自动从 ModelScope 下载模型
# 若 funasr 下载模型失败，取消下一行注释重装：
# modelscope
